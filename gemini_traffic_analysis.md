<!--
Generated by gemini_traffic_analyzer.py
Mode: Gemini API
Model: gemini-3.8-flash
Source: adaptive_demo_results.csv
-->

# Gemini Traffic Operations Analysis

## Executive Summary
This report analyzes aggregated data from a 1,000-step SUMO adaptive traffic-control simulation across four intersections (`TLS_1` through `TLS_4`), encompassing 4,000 recorded decision points. The data exhibits a sharp operational divide: `TLS_1` and `TLS_2` account for the vast majority of vehicle delay and negative reward values, with frequent green-phase truncation, while `TLS_3` and `TLS_4` operate with low queuing and zero green truncation. These metrics reflect simulated model conditions rather than real-world municipal telemetry.

## Congestion Observations
- **Red-Phase Halting Disparity:** Severe red-phase halting is concentrated at `TLS_1` (average 28.53 vehicles, peaking at 71.0) and `TLS_2` (average 29.61 vehicles, peaking at 63.0). In contrast, `TLS_3` and `TLS_4` maintain minimal red-phase queuing, averaging 1.66 (max 7.0) and 2.62 (max 8.0) vehicles, respectively.
- **Green-Phase Halting:** Across all intersections, vehicles halted during green phases remain minimal, with an overall average of 0.04 vehicles. `TLS_4` recorded the highest individual green halting (average 0.11, max 7.0), while `TLS_1` and `TLS_2` averaged 0.01.
- **Queue Accumulation:** The high maximum red halting values at `TLS_1` (71.0) and `TLS_2` (63.0) indicate substantial intermittent or sustained queue buildup on stopped approaches.

## Controller Behaviour
- **Action Selection Split:** 
  - `TLS_1` and `TLS_2` are dominated by `SHORTEN_GREEN` decisions, accounting for 757 and 764 out of 1,000 steps, respectively (approx. 76% of all decisions per intersection). `KEEP` was chosen only 173 and 166 times.
  - `TLS_3` and `TLS_4` recorded zero `SHORTEN_GREEN` actions. Their decisions consisted primarily of `KEEP` (638 and 592 steps) and `MIN_GREEN` (296 and 342 steps).
  - `TRANSITION_UNCHANGED` occurred infrequently across the network (156 total: 12 each at `TLS_1`/`TLS_2`, and 66 each at `TLS_3`/`TLS_4`).
- **Reward Correlation:** The average reward is heavily penalized at `TLS_1` (-1,220.193) and `TLS_2` (-1,359.104), directly reflecting the prolonged halting metrics on these nodes, compared to `TLS_3` (-14.421) and `TLS_4` (-22.772).

## Intersections Requiring Attention
- **`TLS_1` and `TLS_2`:** Both intersections require immediate operational review. Despite substantial queuing during red phases (averages exceeding 28 vehicles), the adaptive policy persistently selects `SHORTEN_GREEN`.

## Suggested Investigation
Human operators and simulation researchers should investigate:
1. **Controller Decision Logic at `TLS_1` and `TLS_2`:** Examine why the policy repeatedly selects `SHORTEN_GREEN`. Verify whether the controller detects empty green phases while conflicting red phases hold large queues, or if the policy is prematurely cutting green time needed for clearance.
2. **Phase Definitions and Detector Mapping:** Check whether detectors on the congested approaches are accurately registered in the simulation controller's state representation.
3. **Reward Function Formulations:** Review how red-phase halting counts and phase switching penalties are weighted, as current penalties (-1,220+ avg) suggest the policy is failing to find an effective strategy for these two intersections.
4. **Demand and Network Topology:** Evaluate whether high inflow rates, network bottlenecks, or downstream blockages between `TLS_1` and `TLS_2` are causing sustained queue holdover.

## Safety and Scope Note
All findings in this brief are derived exclusively from offline simulation logs (`adaptive_demo_results.csv`) generated within SUMO and do not reflect live municipal infrastructure or real-world traffic conditions. This analysis is purely advisory for simulation researchers and does not provide operational signal timing plans or actuation commands.
